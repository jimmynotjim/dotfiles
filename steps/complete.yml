- defaults:
    link:
      relink: true
      create: true
    shell:
      quiet: true
      stdout: true
      stderr: true

- shell:
    - command: |
        echo '
        ----------------------------------------------------------------
        ✅ Installation complete!

        To activate mise and use Node.js immediately, run:
          exec zsh

        Or simply open a new terminal window.
        ----------------------------------------------------------------
        Follow the remaining instructions for manual setup in the README
        ----------------------------------------------------------------
        '
      description: ""
      quiet: true
      stdout: true
